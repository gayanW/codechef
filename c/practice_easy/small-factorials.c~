/*
 * LANG: C
 * CODE: FCTRL2
 */  

#include <stdio.h>

int main()
{
  int t, n, i, factorial = 1;

  scanf("%d", &t);
  while (t--) {
    scanf("%d", &n);
    i = 1, factorial = 1;
    
    while (i <= n) {
      factorial *= i;
      ++i;
    }

    printf("%d\n", factorial);
  }

  return 0;
}
